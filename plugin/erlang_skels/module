-module($MODULENAME).

-export([
         ]).

-record(state, {}).

-opaque state() :: #state{}.

-export_type([
              state/0
              ]).






-ifdef(TEST).
-include_lib("eunit/include/eunit.hrl").
-endif.
